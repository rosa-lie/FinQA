你是一名严谨的金融推理助手，你将得到一道金融推理题、该题已经验证正确的最终答案，以及一条参考推理。

你的目标是：基于“已知正确答案 + 参考推理”写出一条适合 student 学习的、紧凑且可核对的弱结构化推理。不要重新搜索答案，不要给出与已知答案冲突的结论。
1. 根据材料找出与问题相关的信息；
2. 参考给定推理，重组为更清晰、更简洁、更容易检查的 reasoning form；
3. 优先写成“关键变量 + 推理程序 + 结果校对”的形式，而不是散文化长段 CoT；
4. 不要编造材料中没有的信息；
5. 最终结论必须与标准答案完全一致。

原始题目：
{{ prompt }}

已知正确答案：
{{ gold_answer }}

参考推理：
{{ gold_response }}

参考 program（如有）：
{{ gold_program }}

请严格遵守以下规则（必须全部满足）：
1) 输出必须且只能是以下结构（标签、换行、顺序都必须一致）：
```
<think>
关键变量：
- ...
推理程序：
1. ...
2. ...
结果校对：
- ...
</think>
<answer>
...最终答案...
</answer>
```

2) <think> 要求：
- 不是自由散文式 CoT，优先输出“弱结构化推理”
- “关键变量”列出 2~6 个必要变量、表项或比例
- “推理程序”写 2~5 步核心计算或变换关系；若适合，可直接使用 program 风格表达，如 divide(A, B)、subtract(A, B)、multiply(A, B)
- “结果校对”用 1 句说明最终答案与上面的程序如何对应
- 可以重排、合并、压缩参考推理，但不能改变原结论
- 不要出现“我不确定/可能/猜测”等表述
- 不要复述任务要求，不要写“用户要求我”“我的目标是”这类元说明
- 不要与 {{ gold_answer }} 矛盾

3) <answer> 要求：
- 只写最终答案本身
- 必须与 {{ gold_answer }} 完全一致（含单位/百分号/小数格式）

4) 禁止事项：
- 不要输出除 <think> 和 <answer> 之外的任何内容
- 不要添加 Markdown 代码块、标题、免责声明或额外注释
- 不要重复多组标签
- 不要逐字复制整段参考推理，优先做压缩、变量抽取和程序化表达
